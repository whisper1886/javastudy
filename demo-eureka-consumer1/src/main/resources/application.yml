server:
  port:7003
spring:
  application:
    name: eureka-consumer1
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7000/eureka/
    instance:
      hostname: localhost